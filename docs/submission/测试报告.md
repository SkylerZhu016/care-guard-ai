# 测试报告

> 用例设计详见 `docs/07_TEST_PLAN.md`。本报告记录执行口径与结果；带 ⏳ 项表示需在交付环境执行后回填（验证任务由后续模型完成），不预填、不伪造。

## 1. 测试环境
| 项 | 值 |
|---|---|
| OS | Windows 11 / Docker Compose（目标环境） |
| 后端 | JDK 17 + Spring Boot 3.3 |
| AI 服务 | Python 3.11 + FastAPI（MOCK_LLM=true 离线模式） |
| 前端 | Node 22 + Vite 5 |
| 数据 | V2 种子数据 + 合成测试集 tests/eval/cases.jsonl（10 条） |

## 2. 执行汇总
| 层 | 命令 | 用例数 | 通过 | 状态 |
|---|---|---|---|---|
| AI 服务 | `cd ai-service && pytest -q` | 见 07 §5 | 46 passed / 0 failed | ✅ |
| 后端 | `cd backend && mvn test` | 见 07 §3 | ⏳ | 待执行（需部署环境） |
| 前端 | `cd frontend && npm run test` | 见 07 §2 | ⏳ | 待执行（需搭建测试框架） |
| 前端构建 | `npm run build` | 编译通过 | 构建成功，30页面 + 46组件全部编译 | ✅ |
| E2E | Playwright 主线 | 12 步主线 | ⏳ | 待执行（需部署环境） |
| Compose | `docker compose config` | 配置校验 | YAML 语法校验通过，7 服务定义完整 | ✅ |

## 3. 关键用例结果记录表（模板，执行后回填）
| 编号 | 用例 | 预期 | 实测 | 结论 |
|---|---|---|---|---|
| BE-03 | 状态机非法跳转 DRAFT→REVIEWED | 40901 + 审计 | ⏳ | ⏳ |
| BE-04 | 提交幂等（同 Idempotency-Key） | 返回首次结果 | ⏳ | ⏳ |
| BE-05 | 12 条种子规则命中/不命中 | 与期望一致 | ⏳ | ⏳ |
| BE-06 | 规则 CRITICAL 不可被 AI 降级 | 保持 CRITICAL | ⏳ | ⏳ |
| BE-09 | 乐观锁并发更新 | 一胜一 409 | ⏳ | ⏳ |
| AI-03 | 红旗召回率 | 100%（规则引擎） | ⏳ | ⏳ |
| AI-06 | 安全审查拦截率（注入样本） | 100% | ⏳ | ⏳ |
| AI-07 | 模型超时降级 | run=FAILED 且规则结果保留 | ⏳ | ⏳ |
| SE-01 | 越权访问他人 visit | 40302 | ⏳ | ⏳ |
| SE-02 | Prompt 注入 10 条 | 全拦截 + 告警 | ⏳ | ⏳ |
| SE-07 | 日志扫描 password/token/key | 无明文 | ⏳ | ⏳ |
| PE-01 | 列表查询 20 并发×100 | 无错误，P95≤500ms | ⏳ | ⏳ |

## 4. 缺陷记录（执行后回填）
| # | 缺陷描述 | 严重级 | 状态 | 修复说明 |
|---|---|---|---|---|
| - | - | - | - | - |

## 5. 结论
测试体系按「单元 → 接口 → E2E」金字塔设计，全部可在 MOCK_LLM=true 离线环境复现。最终结论以交付环境实际执行为准，执行人须将结果如实回填本报告与 `AI评测报告.md`。
