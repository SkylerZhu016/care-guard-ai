[project]
name = "medsim-ai-service"
version = "1.0.0"
requires-python = ">=3.9,<3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q --cov=app --cov-report=term-missing --cov-fail-under=75"

